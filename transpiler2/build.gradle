repositories {
    mavenCentral()
}

dependencies {
    implementation project(':parser2')
    compile group: 'org.mdkt.compiler', name: 'InMemoryJavaCompiler', version: '1.3.0'
    testCompileOnly(
            'junit:junit:4.12'
    )
}